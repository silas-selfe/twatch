# Copy to .env (gitignored -- NEVER commit the real one).
# The node login you created with: CREATE ROLE node_... LOGIN ... IN ROLE tw_node;
TW_CENTRAL_DSN=postgresql://node_home_window:PASSWORD@YOUR-RDS-ENDPOINT:5432/twatch?sslmode=require
